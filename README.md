# safeoutput-java

Safe Output 是面向 Spring Boot 2.x / Java 8 的输出侧脱敏组件。主工程位于 `safe-output/`，提供 response 脱敏、Log4j2 日志脱敏、主动脱敏、聚合报告和 R2 Demo 验证接口。

## 快速验证

```sh
cd safe-output
mvn test
```

完整说明见 `safe-output/README.md`。
