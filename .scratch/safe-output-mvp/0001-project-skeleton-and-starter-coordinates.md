# 建立 Maven 多模块与 Starter 坐标基线

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: doc/prd/safe-output-mvp-prd.md
- wbs: WBS-00, WBS-13
- blocked_by: None

## What to build

建立 Safe Output Maven 多模块工程骨架，明确 JDK8、Spring Boot 2.x、starter 聚合依赖、artifact 坐标和基础构建约束。最终必须能生成 `safe-output-spring-boot-starter` 入口包。

## Acceptance criteria

- [x] 根工程和子模块结构符合 PRD 推荐项目结构。
- [x] 所有模块使用 JDK8 编译约束，不使用 Java 9+ API。
- [x] `safe-output-spring-boot-starter` 具备明确 Maven 坐标和版本。
- [x] starter 能声明并聚合 core、log4j2、report 内部模块依赖。
- [x] 预留 Spring Boot 2.x `META-INF/spring.factories` 自动装配入口。

## Test requirements

- 执行 Maven 基础构建验证。
- 检查依赖树中没有强制覆盖宿主 Spring Boot 版本的重依赖。
