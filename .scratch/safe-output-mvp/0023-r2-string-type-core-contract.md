# 将核心脱敏类型标签改为 String

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: doc/prd/safe-output-r2-prd.md
- wbs: R2-04
- blocked_by: None

## What to build

将核心扩展层的类型标签统一改为 String，使内置类型和业务自定义类型能贯穿规则、注解、策略、上下文、结果和统计链路。保留 `MaskType` 作为内置清单或兼容来源，并新增 `MaskTypes` 字符串常量作为推荐使用方式。

## Acceptance criteria

- [x] `@Desensitize.type` 支持 String 类型标签。
- [x] `MaskStrategy.type()` 返回 String 类型标签。
- [x] `MaskContext`、`MaskResult`、`MaskRule`、`RuleMatch` 中的脱敏类型字段支持 String。
- [x] `MaskStrategyRegistry` 使用 String 作为注册和查找 key。
- [x] 新增 `MaskTypes` 常量类，包含 MVP 内置标准类型。
- [x] 内置 `MOBILE`、`ID_CARD`、`EMAIL` 等类型继续可用。
- [x] 现有默认规则库仍能引用内置标准类型。

## Blocked by

None - can start immediately

## Test requirements

- 覆盖核心接口和模型使用 String type。
- 覆盖 `MaskTypes` 常量与内置策略注册一致。
- 覆盖现有内置策略测试迁移后保持通过。
