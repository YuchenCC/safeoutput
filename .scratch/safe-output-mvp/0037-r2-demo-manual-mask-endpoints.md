# 增加主动脱敏 Demo 验证接口

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r2-prd.md
- wbs: R2-08
- blocked_by: 0032-r2-manual-scene-metrics.md

## What to build

在 Demo 中增加主动脱敏验证接口，覆盖指定 type、对象规则和强扫描三种模式。接口响应需要展示第一次脱敏、第二次脱敏和幂等性判断，便于验证、复现和截图。

## Acceptance criteria

- [x] `POST /demo/mask/by-type` 可验证指定 type 脱敏。
- [x] `POST /demo/mask/object` 可验证对象规则脱敏。
- [x] `POST /demo/mask/strong` 可验证强扫描脱敏。
- [x] 每个接口展示第一次脱敏和第二次脱敏结果。
- [x] 每个接口展示 `idempotent=true/false`。
- [x] 对象规则示例中 `realName` 和 `mobile` 脱敏，`name` 商品字段不误脱敏。
- [x] Demo 包含自定义 `mobileM` 策略示例。
- [x] 主动调用进入 MANUAL 场景统计。

## Blocked by

- 0032-r2-manual-scene-metrics.md

## Test requirements

- 覆盖三个 Demo 接口的 HTTP 集成测试。
- 覆盖幂等性字段。
- 覆盖 Demo 不把敏感原文写入统计报告。
