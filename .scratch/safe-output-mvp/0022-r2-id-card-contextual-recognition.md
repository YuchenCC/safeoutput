# 优化身份证上下文识别策略

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r2-prd.md
- wbs: R2-02
- blocked_by: None

## What to build

按上下文优化身份证脱敏识别。Response、注解、配置 path、日志 key-value 和主动脱敏显式命中 `ID_CARD` 时直接执行脱敏，优先避免明文输出；日志无上下文兜底场景继续做轻量格式、日期和年份校验，不默认引入行政区划校验或外部区划库。

## Acceptance criteria

- [x] Response 中 `idCard` 字段命中后直接脱敏。
- [x] Response 中字段明确为身份证时，即使值格式不完全合法也执行脱敏。
- [x] 日志中 `idCard=350102199001011234` 这类明确 key-value 可直接脱敏。
- [x] 日志中孤立出现的疑似身份证号，经轻量校验后可脱敏。
- [x] 日志中明显不符合身份证日期格式的 18 位字符串不脱敏。
- [x] 校验位校验可配置启用，默认启用。
- [x] 行政区划校验默认关闭，且不引入外部行政区划库强依赖。

## Blocked by

None - can start immediately

## Test requirements

- 覆盖明确上下文下合法和不完全合法身份证值均脱敏。
- 覆盖日志 fallback 的合法日期、非法日期、年份范围和校验位配置。
- 覆盖行政区划校验默认不作为必选项。
