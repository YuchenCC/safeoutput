# 主动脱敏实验室页面

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r2-prd.md §11 R3-01
- wbs: R3-01
- blocked_by: 0040-r3-frontend-scaffold-and-dashboard.md

## Current status

Absorbed by 0049-r25-manual-mask-lab-and-performance.md.
The latest lab page is `#lab`, with three active panels for by-type, object, and strong text masking. The backend response format was intentionally changed from the old `original/masked/secondMasked/idempotent/type/elapsed` shape to a fixed two-round array: `{round,result,elapsedNanos,sameAsPrevious}`.

Current implementation uses `SafeOutputMaskService` through `/demo/mask/by-type`, `/demo/mask/object`, and `/demo/mask/strong`. The page shows round results, elapsed time, and second-round stability. The older "Tab" wording is treated as equivalent to the current three-entry lab layout.

## What to build

实现主动脱敏实验室页面，提供三个 Tab：指定 type 脱敏、对象规则脱敏、强扫描脱敏。每个 Tab 展示输入、第一次脱敏结果、第二次脱敏结果、幂等性判断（idempotent=true/false）、命中的策略 type 和耗时。后端已有 `/demo/mask/*` 接口，需确认接口返回格式满足前端展示需求。

## Acceptance criteria

- [x] 页面提供三类入口：指定 type 脱敏、对象规则脱敏、强扫描脱敏。
- [x] 每类入口展示输入和第一次脱敏结果。
- [x] 每类入口展示第二次脱敏结果。
- [x] 每类入口展示二次脱敏稳定性判断（sameAsPrevious）。
- [x] 每类入口展示脱敏耗时。
- [x] 后端接口返回格式满足当前前端展示需求（固定两轮 `{round,result,elapsedNanos,sameAsPrevious}` 数组）。
- [x] 命中的策略 type 不再作为每轮响应字段单独返回；按当前输入 type、对象规则或 strong scan 场景表达。

## Blocked by

- 0040-r3-frontend-scaffold-and-dashboard.md

## Test requirements

- [x] 验证 `#lab` 三类入口渲染和交互。
- [x] 验证指定 type 脱敏可输入 value 和 type 并展示两轮结果。
- [x] 验证对象规则脱敏可输入业务对象字段并展示两轮结果。
- [x] 验证强扫描脱敏可输入文本并展示两轮结果。
- [x] 验证接口返回固定两轮 `{round,result,elapsedNanos,sameAsPrevious}`。
