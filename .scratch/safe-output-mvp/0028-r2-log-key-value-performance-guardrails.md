# 增加日志 key-value 匹配性能保护

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r2-prd.md
- wbs: R2-05
- blocked_by: 0027-r2-log-key-value-rule-masking.md

## What to build

为日志 key-value 规则匹配增加高频路径性能保护。初始化时构建 key 映射和匹配缓存，不允许每条日志动态拼接并编译正则；超出限制时安全跳过或降级，日志脱敏异常不得影响日志输出。

## Acceptance criteria

- [x] 支持 `safe-output.log.key-value-rule-enabled` 开关。
- [x] 支持最大日志长度限制，超限时不影响日志输出。
- [x] 支持单个 value 最大扫描长度限制，超限时安全跳过或降级。
- [x] 支持参与日志匹配的 rule key 数量限制。
- [x] 初始化阶段构建 key -> type 映射和必要缓存。
- [x] 每条日志处理过程中不动态编译规则集合。
- [x] 日志脱敏异常不影响应用正常输出。
- [x] 不引入 fastjson、Jackson 等 JSON Parser 作为日志脱敏强依赖。
- [x] 关闭 `key-value-rule-enabled` 时不执行配置 key-value 匹配；若 fallback 开启，fallback 仍独立生效。
- [x] Spring Boot starter 配置的 `max-message-length`、`max-value-length`、`max-rule-keys` 会驱动真实 `%safeOutputMsg`。
- [x] `Pattern.compile` 不在单条日志处理路径动态执行，key 映射在 masker 初始化时构建；该项通过代码审查确认。

## Blocked by

- 0027-r2-log-key-value-rule-masking.md

## Test requirements

- 覆盖 message 长度、value 长度和 key 数量超限。
- 覆盖异常降级仍返回原日志或可用日志。
- 覆盖配置开关关闭时不执行 key-value 规则匹配。
- 覆盖真实 `%safeOutputMsg` starter 集成路径中的 `safe-output.log.*` 限制。
- 覆盖 key-value 关闭但 fallback 开启时，两者开关互不绑死。
- 代码审查确认日志处理路径不动态拼接并编译规则集合。
