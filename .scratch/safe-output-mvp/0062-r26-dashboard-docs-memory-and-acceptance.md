# R2.6 Dashboard 文档、记忆与验收校准

> *This was generated by AI during triage.*

- category: enhancement
- state: ready-for-human
- source: doc/prd/safe-output-r26-dashboard-prd.md
- wbs: R2.6-08
- blocked_by: 0061-r26-demo-adopts-dashboard-starter.md

## What to build

完成 R2.6 Dashboard 附加包的交付校准。更新接入文档、Demo 说明、运行命令、测试命令和 `.codex-memory` 中受影响的模块地图、核心调用链、设计边界与下一轮交接信息，确保后续 agent 能准确区分 Demo 业务系统与可复用 Dashboard starter。

本切片同时执行端到端验收，确认 Dashboard 默认关闭、显式开启、API 全 POST、安全边界、报告上传不落盘、Demo 兼容接口和测试命令都与 PRD 一致。

## Acceptance criteria

- [ ] 文档说明 Dashboard starter 是默认关闭的可选附加包。
- [ ] 文档说明默认路径前缀、自定义路径前缀、API 全 POST 和静态资源 GET 边界。
- [ ] 文档说明 Dashboard 不内置登录、权限、角色、多租户、审计、数据库或公网保护。
- [ ] 文档说明接入方必须自行通过内网、网关、Spring Security 或运维策略保护 Dashboard 入口。
- [ ] 文档说明 Demo 业务工作台、小眼睛明文查看和 mock 域不属于通用 Dashboard。
- [ ] `.codex-memory` 同步更新新增模块、运行命令、测试命令、Demo 能力和 R2.6 交接信息。
- [ ] 验收确认 Dashboard 页面、报告查看和上传报告不展示原始 response、完整日志 message 或敏感命中值。
- [ ] 验收确认旧 `/demo/report/**` 和 `/demo/mask/**` 第一阶段仍可用。

## Blocked by

- 0061-r26-demo-adopts-dashboard-starter.md

## Test requirements

- [ ] 运行 `mvn -pl safe-output-dashboard-spring-boot-starter -am test`。
- [ ] 运行 `mvn -pl safe-output-demo -am test`。
- [ ] 如改动 starter、report 或 core，同步运行对应受影响模块测试。
- [ ] 发布前运行 `mvn verify`。
