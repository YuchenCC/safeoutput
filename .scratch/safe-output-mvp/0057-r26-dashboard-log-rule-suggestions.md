# R2.6 Dashboard 日志规则建议视图

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: doc/prd/safe-output-r26-dashboard-prd.md
- wbs: R2.6-03
- blocked_by: 0055-r26-dashboard-starter-autoconfiguration-shell.md

## Completion notes

已完成并复核。Dashboard starter 提供 `/log-suggestions` POST API 和页面视图，复用 `LogRuleSuggestionAnalyzer` 与 `SafeOutputConfiguredKeys` 过滤已配置 key，返回建议 key、类型标签、置信度、脱敏 evidence 摘要和默认 `enabled:false` 的 YAML 候选片段。实现不提供自动采纳、不修改宿主 YAML、不做在线规则热更新，也不展示完整日志或敏感原文。

## What to build

在 Dashboard starter 中提供日志规则建议的端到端视图。接入方启用 Dashboard 后，可以查看当前应用聚合出的日志敏感 key 线索、建议类型标签、置信度和默认关闭的 YAML 候选片段，用于人工复核后再纳入配置。

本切片不做规则自动采纳，不修改宿主 YAML，不保存完整日志 message，也不改变 Log4j2 在线脱敏规则优先级。

## Acceptance criteria

- [x] Dashboard 提供 POST API 返回日志规则建议列表、建议类型标签、置信度和 evidence 摘要。
- [x] Dashboard 提供默认 `enabled:false` 的 YAML 候选片段，供人工复核。
- [x] 建议数据复用现有 `LogRuleSuggestionAnalyzer` 和 configured-key 过滤逻辑。
- [x] Dashboard 页面可以渲染日志规则建议和 YAML 候选片段。
- [x] 页面和 API 不显示完整日志 message 或敏感原文。
- [x] Dashboard 不提供自动采纳、自动修改配置或在线规则热更新能力。
- [x] 低置信度建议可展示但必须保持人工确认语义。

## Blocked by

- 0055-r26-dashboard-starter-autoconfiguration-shell.md

## Test requirements

- [x] 覆盖建议列表、置信度、建议类型和默认关闭 YAML 片段。
- [x] 覆盖已配置 key 不重复进入治理建议。
- [x] 覆盖 API 响应不包含完整日志 message 或敏感值。
- [x] 覆盖 GET 调用日志建议 API 不可用。
- [x] 运行 `mvn -pl safe-output-dashboard-spring-boot-starter -am test`，2026-05-21 通过，dashboard starter 9 tests，reactor BUILD SUCCESS。
