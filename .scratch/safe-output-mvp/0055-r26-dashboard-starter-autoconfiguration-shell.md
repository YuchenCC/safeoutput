# R2.6 Dashboard 可选 starter 与自动装配壳层

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r26-dashboard-prd.md
- wbs: R2.6-01
- blocked_by: None

## Completion notes

已完成并复核。`safe-output-dashboard-spring-boot-starter` 已加入 Maven reactor，作为默认关闭的可选附加包；自动装配受 `safe-output.dashboard.enabled=true`、Servlet Web 应用和 `DispatcherServlet` 条件约束。默认入口为 `/safe-output/dashboard/index.html`，API 位于 `/safe-output/dashboard/api/...` 且只通过 POST 暴露；自定义 `safe-output.dashboard.path-prefix` 会同步移动静态入口和 API。Dashboard starter 不依赖 demo，也不包含 Demo mock 域或小眼睛明文能力。

## What to build

新增 `safe-output-dashboard-spring-boot-starter` 可选附加包的最小可运行闭环。接入方引入该附加包后，Dashboard 默认不暴露任何 API 或页面；只有在 Spring MVC Web 应用中显式配置 `safe-output.dashboard.enabled=true` 时，才按配置前缀暴露本地 Dashboard 静态入口和 POST API 基础路由。

本切片只建立模块、配置属性、自动装配条件、路径前缀解析、静态页面壳层和最小健康信息，不迁入 Demo 业务域，也不实现完整治理数据。

## Acceptance criteria

- [x] Maven reactor 包含新的 Dashboard starter 模块，保持 Java 8 与 Spring Boot 2.x 兼容。
- [x] Dashboard starter 不依赖 `safe-output-demo`，不包含客户、订单、支付、工单、账户 mock 数据或小眼睛明文查看能力。
- [x] 默认配置下引入 Dashboard starter 不注册 Dashboard API、Controller 或静态入口。
- [x] `safe-output.dashboard.enabled=true` 且处于 Spring MVC Web 应用时，Dashboard 使用默认前缀 `/safe-output/dashboard` 暴露入口。
- [x] `safe-output.dashboard.path-prefix` 可以调整静态入口和 API 前缀，API 统一位于 `{path-prefix}/api/...`。
- [x] 非 Web 应用或非 Spring MVC 场景不暴露 Dashboard API。
- [x] Dashboard 后端 API 基础路由只接受 POST；静态页面和静态资源可以通过 GET 加载。
- [x] 失败或条件不满足时不影响宿主业务启动。

## Blocked by

None - can start immediately

## Test requirements

- [x] 覆盖默认不启用时不会注册 Dashboard 暴露面的自动装配测试。
- [x] 覆盖显式启用、默认路径前缀、自定义路径前缀的自动装配测试。
- [x] 覆盖非 Web 或非 Spring MVC 场景不暴露 Dashboard API。
- [x] 覆盖 Dashboard API 的 GET 调用不可用，静态资源 GET 可用。
- [x] 运行 `mvn -pl safe-output-dashboard-spring-boot-starter -am test`，2026-05-21 通过，dashboard starter 9 tests，reactor BUILD SUCCESS。
