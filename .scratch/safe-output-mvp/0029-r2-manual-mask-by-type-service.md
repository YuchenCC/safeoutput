# 新增指定 type 主动脱敏服务

> *This was generated by AI during triage.*

- category: enhancement
- state: ready-for-agent
- source: doc/prd/safe-output-r2-prd.md
- wbs: R2-06
- blocked_by: 0026-r2-custom-strategy-end-to-end.md

## What to build

新增主动脱敏服务接口的指定类型模式，让宿主系统可注入 `SafeOutputMaskService` 并调用 `mask(value, type)` 对明确类型的值执行脱敏。该模式只根据 type 查找策略，不做字段规则匹配或正则扫描。

## Acceptance criteria

- [x] 宿主系统可注入 `SafeOutputMaskService`。
- [x] 调用 `mask("13812345678", "MOBILE")` 返回手机号脱敏结果。
- [x] 调用 `mask("张三", "CHINESE_NAME")` 返回姓名脱敏结果。
- [x] 调用 `mask("13812345678", "mobileM")` 可命中自定义策略。
- [x] 未知 type 按 unknown type 策略 warn + DEFAULT fallback。
- [x] 主动调用异常不影响宿主业务主流程。
- [x] 核心逻辑通过 Spring Bean 和统一服务实现，不以纯 static 工具类作为核心实现。

## Blocked by

- 0026-r2-custom-strategy-end-to-end.md

## Test requirements

- 覆盖内置 type、自定义 type、unknown type。
- 覆盖服务自动装配和注入。
- 覆盖异常 fail-open。
