# R2.6 Dashboard 通用脱敏实验室

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r26-dashboard-prd.md
- wbs: R2.6-06
- blocked_by: 0055-r26-dashboard-starter-autoconfiguration-shell.md

## Completion notes

已完成并复核。Dashboard starter 提供 `/lab/by-type`、`/lab/object`、`/lab/strong` 三个 POST API，复用宿主应用真实 `SafeOutputMaskService`。三类实验固定执行两轮，返回脱敏结果、`elapsedNanos` 和 `sameAsPrevious`，并进入 `MANUAL` 统计；响应只返回脱敏结果和必要执行信息，不回传原始输入。对象实验验证当前规则、注解和 ignore 行为，强文本扫描用于展示主动 strong scan 边界。

## What to build

在 Dashboard starter 中提供通用脱敏实验室，支持按类型标签脱敏、对象脱敏和强文本扫描三类标准能力。实验室必须调用当前应用真实 `SafeOutputMaskService`、规则配置、注解、ignore、策略注册和强文本扫描能力，让接入方验证内置与自定义类型标签的运行效果。

每次实验固定执行两轮，返回脱敏结果、每轮耗时和二次脱敏稳定性；实验室调用计入 `MANUAL` 场景统计。响应不得返回用户输入原文。

## Acceptance criteria

- [x] Dashboard 提供 POST API 执行按类型标签脱敏，支持内置和自定义类型标签。
- [x] Dashboard 提供 POST API 执行对象脱敏，验证当前规则、注解和 ignore 行为。
- [x] Dashboard 提供 POST API 执行强文本扫描，展示主动 strong scan 与 Response 自动脱敏的边界。
- [x] 三类实验均固定执行两轮，返回每轮脱敏结果、`elapsedNanos` 和 `sameAsPrevious`。
- [x] 实验室调用计入 `MANUAL` 场景统计。
- [x] API 响应不返回原始输入，只返回脱敏结果、耗时、稳定性和必要规则摘要。
- [x] 页面可以发起三类实验并展示两轮结果和耗时。
- [x] 实验室不改变 `MaskRuleMatcher` 优先级，不把未知 type 自动改写成内置 `DEFAULT` 配置。

## Blocked by

- 0055-r26-dashboard-starter-autoconfiguration-shell.md

## Test requirements

- [x] 覆盖按类型标签脱敏实验。
- [x] 覆盖对象脱敏实验。
- [x] 覆盖强文本扫描实验。
- [x] 覆盖两轮结果、`elapsedNanos` 和 `sameAsPrevious`。
- [x] 覆盖实验室调用进入 `MANUAL` 场景统计。
- [x] 覆盖响应不包含原始输入。
- [x] 运行 `mvn -pl safe-output-dashboard-spring-boot-starter -am test`，2026-05-21 通过，dashboard starter 9 tests，reactor BUILD SUCCESS。
