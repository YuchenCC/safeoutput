# 新增强扫描主动脱敏服务

> *This was generated by AI during triage.*

- category: enhancement
- state: ready-for-agent
- source: doc/prd/safe-output-r2-prd.md
- wbs: R2-06
- blocked_by: 0027-r2-log-key-value-rule-masking.md, 0029-r2-manual-mask-by-type-service.md

## What to build

为 `SafeOutputMaskService` 增加强扫描模式，允许业务方显式调用 `maskStrong` 或 `maskObjectStrong` 对文本和对象中的字符串执行类似 Log 的脱敏处理。默认扫描 MOBILE、EMAIL、ID_CARD，允许一定误脱敏。

## Acceptance criteria

- [ ] 调用 `maskStrong(text)` 时，可脱敏文本中的手机号、邮箱、身份证。
- [ ] 强扫描支持 key-value 轻量识别。
- [ ] 强扫描支持 value 内轻量扫描和通用正则兜底。
- [ ] `maskObjectStrong` 可对对象中的字符串执行强扫描。
- [ ] 强扫描必须由业务方显式调用。
- [ ] 默认扫描类型为 MOBILE、EMAIL、ID_CARD。
- [ ] 默认不扫描 BANK_CARD、CHINESE_NAME、ADDRESS，除非显式配置开启。
- [ ] 强扫描异常不影响宿主业务主流程。

## Blocked by

- 0027-r2-log-key-value-rule-masking.md
- 0029-r2-manual-mask-by-type-service.md

## Test requirements

- 覆盖文本、Map、Bean 中的强扫描。
- 覆盖默认扫描类型和默认不扫描类型。
- 覆盖 key-value 识别与 fallback 兜底。
