# R2.6 Dashboard 实时概览与接口风险画像

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: doc/prd/safe-output-r26-dashboard-prd.md
- wbs: R2.6-02
- blocked_by: 0055-r26-dashboard-starter-autoconfiguration-shell.md

## Completion notes

已完成并复核。Dashboard starter 提供 `/overview` 和 `/response-risk` POST API，直接读取宿主应用 `MaskMetricsCollector` 快照与 Response 风险聚合，返回总量、场景分布、类型标签、失败次数、耗时、高风险接口、ignore 接口和慢接口等聚合视图。页面资源在 dashboard starter 内渲染这些聚合字段，不依赖 Demo API，不返回原始 response、完整日志或敏感命中值；读取异常时返回空聚合视图。

## What to build

在 Dashboard starter 中提供实时治理概览和接口风险画像的端到端视图。接入方启用 Dashboard 后，可以在本地页面查看当前进程真实 `MaskMetricsCollector` 快照、Response/Log/Manual 场景分布、类型标签 Top、fail-open 失败次数、耗时指标，以及高风险接口、ignore 接口和慢接口提示。

页面与 API 必须复用当前应用真实统计和风险模型，只展示聚合信息，不展示原始 response、完整日志 message 或敏感命中值。

## Acceptance criteria

- [x] Dashboard 提供 POST API 返回当前进程实时脱敏总量、场景分布、类型标签统计、失败次数、平均耗时和最大耗时。
- [x] Dashboard 提供 POST API 返回接口风险统计，包括高风险接口、ignore 接口和慢接口提示。
- [x] 实时概览复用宿主应用中的 `MaskMetricsCollector`，不创建与业务脱敏链路脱节的独立统计源。
- [x] 接口风险画像复用现有 Response 风险聚合模型。
- [x] Dashboard 页面可以渲染实时概览和接口风险画像，且不依赖 Demo API。
- [x] API 响应和页面不返回、不显示原始 response、完整日志或敏感命中值。
- [x] Dashboard API 使用 POST body 传参，不使用 URL query 暴露筛选条件。
- [x] 脱敏统计读取异常时 fail-open 为可诊断的空视图或错误摘要，不影响宿主业务接口。

## Blocked by

- 0055-r26-dashboard-starter-autoconfiguration-shell.md

## Test requirements

- [x] 覆盖总脱敏次数、场景分布、类型标签计数、失败次数和耗时指标。
- [x] 覆盖高风险接口、ignore 接口和慢接口聚合展示。
- [x] 覆盖 GET 调用实时治理 API 不可用。
- [x] 覆盖 API 响应不包含敏感原文样例。
- [x] 运行 `mvn -pl safe-output-dashboard-spring-boot-starter -am test`，2026-05-21 通过，dashboard starter 9 tests，reactor BUILD SUCCESS。
