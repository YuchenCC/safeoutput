# R2.6 Dashboard 上传报告临时查看

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r26-dashboard-prd.md
- wbs: R2.6-05
- blocked_by: 0058-r26-dashboard-report-directory-and-single-report-view.md

## Completion notes

已完成并复核。Dashboard starter 提供 `/reports/upload` multipart POST API，限制单个 `.json` 报告、1MB 大小和 Safe Output 报告关键字段；非法扩展名、超大文件、结构缺失或关键字段缺失会返回可诊断错误。上传报告只在请求内解析并复用历史报告 dashboard 模型，不写入报告目录，不进入历史列表，不产生上传历史，也不展示 JSON 原文或敏感原文。

## What to build

在 Dashboard starter 中提供单个 JSON 报告上传后的临时可视化查看能力。生产环境可以只启用 report、不启用 Dashboard；开发或运维人员把 JSON 报告带到受控测试环境后，通过 Dashboard 上传并查看聚合图表。

上传报告只做临时解析，不写入报告目录或其他持久化位置，不提供上传历史，也不展示 JSON 原文。

## Acceptance criteria

- [x] Dashboard 提供 POST 上传 API，接受单个 Safe Output JSON 报告快照并返回可视化聚合模型。
- [x] 上传报告校验扩展名、大小、JSON 结构和关键字段。
- [x] 非 JSON 文件、超大文件、结构缺失或非法报告被拒绝，并返回可诊断错误。
- [x] 上传报告不落盘，不进入报告目录列表，不产生上传历史。
- [x] 上传报告视图复用单报告 dashboard 展示模型。
- [x] 页面和 API 不展示 JSON 原文、原始 response、完整日志 message 或敏感命中值。
- [x] 第一阶段不新增 `upload.enabled` 开关，不引入数据库、对象存储或清理策略。

## Blocked by

- 0058-r26-dashboard-report-directory-and-single-report-view.md

## Test requirements

- [x] 覆盖合法报告上传查看。
- [x] 覆盖非 JSON 文件拒绝。
- [x] 覆盖超大文件拒绝。
- [x] 覆盖结构缺失或关键字段缺失拒绝。
- [x] 覆盖上传报告不会写入报告目录。
- [x] 运行 `mvn -pl safe-output-dashboard-spring-boot-starter -am test`，2026-05-21 通过，dashboard starter 9 tests，reactor BUILD SUCCESS。
