# 增加 R2 报告 Demo 验证接口

> *This was generated by AI during triage.*

- category: enhancement
- state: done
- source: docs/project/prd/safe-output-r2-prd.md
- wbs: R2-08
- blocked_by: 0034-r2-response-risk-analysis-report.md, 0036-r2-log-rule-suggestion-report.md

## What to build

在 Demo 中增加 R2 报告查看接口，覆盖 Response 风险画像、Response 性能画像、Log 规则建议和配置片段。接口优先用于需求验证，不构建完整竞赛展示看板。

## Acceptance criteria

- [x] `GET /demo/report/response-risk` 可返回 responseRiskSummary。
- [x] `GET /demo/report/response-risk` 可返回 topRiskApis、riskReasons、governanceAdvice。
- [x] `GET /demo/report/response-risk` 可返回 performanceProfile 和 ignoredRiskApis。
- [x] `GET /demo/report/log-suggestions` 可返回 logRuleSuggestions。
- [x] `GET /demo/report/log-suggestions` 可返回 suggestedType、hitCount、confidence、evidence、effectScopes。
- [x] `GET /demo/report/log-suggestions` 可返回 configSnippet。
- [x] Demo 数据包含 `phoneNo`、`certNum`、`mailAddr` 等未配置 key 场景。
- [x] Demo 报告不保存敏感原文、完整 response 或完整日志。

## Blocked by

- 0034-r2-response-risk-analysis-report.md
- 0036-r2-log-rule-suggestion-report.md

## Test requirements

- 覆盖两个报告接口的 HTTP 集成测试。
- 覆盖报告 JSON 关键字段。
- 覆盖报告安全边界。
