# 接入与配置说明页面及 Agent Skill 预留

> *This was generated by AI during triage.*

- category: enhancement
- state: wontfix
- source: doc/prd/safe-output-r2-prd.md §11 R3-01
- wbs: R3-01
- blocked_by: 0040-r3-frontend-scaffold-and-dashboard.md

## Current status

Superseded by 0048-r25-integration-scenarios-guide.md and the R2.5 frontend design.
The standalone 接入与配置说明页 is no longer the target shape. The latest Demo folds the guide into `#workbench` overview; old `#guide` and `#workbench/integration` routes are compatibility redirects to `#workbench`.

Current guide focuses on business workbench field rules that are actually used: YAML rules, annotation rules, default rules, field ignore, and API ignore. Log4j2 `%safeOutputMsg` and `SafeOutputMaskService` are shown in `#logs` and `#lab` respectively, not mixed into the guide cards. Agent Skill display remains a future reservation and is not part of the latest Demo acceptance path.

## What to build

实现接入与配置说明页面，展示 Maven 依赖、application.yml 示例、log4j2.xml 示例、自定义 MaskStrategy 示例、`@Desensitize` 注解示例、ignore 配置示例和 Agent Skill 预留说明。页面作为竞赛展示的接入引导页，内容可从 README 和文档中提取。

## Acceptance criteria

- [x] 工作台总览展示 application.yml 中业务字段规则示例。
- [x] 工作台总览展示 `@Desensitize` 注解使用示例。
- [x] 工作台总览展示 ignore.keys / ignore.apis 配置示例。
- [x] 工作台总览展示默认规则、YAML、注解、字段 ignore、API ignore 的业务映射。
- [x] 独立接入说明页不再实现，已并入 `#workbench`。
- [x] Log4j2 配置示例改由日志场景和 README 覆盖。
- [x] SafeOutputMaskService 主动脱敏接入改由脱敏实验室覆盖。
- [x] Agent Skill 预留说明区域不再按旧页面实现，保留为后续文档/报告扩展方向。
- [x] 一键复制代码片段不再按旧页面实现。

## Blocked by

- 0040-r3-frontend-scaffold-and-dashboard.md

## Test requirements

- 验证页面渲染和代码片段展示。
- 验证代码片段复制功能。
- 验证示例内容与项目 README 和实际配置一致。
