# 定义核心领域模型与策略 SPI

> *This was generated by AI during triage.*

- category: enhancement
- state: ready-for-human
- source: doc/prd/safe-output-mvp-prd.md
- wbs: WBS-01, WBS-03
- blocked_by: 0001-project-skeleton-and-starter-coordinates.md

## What to build

定义输出侧脱敏的核心领域模型和策略扩展接口，为 response、log、report 等后续能力提供统一抽象。

## Acceptance criteria

- [x] 定义 `MaskType`、`MaskScene`、`MaskContext`、`MaskResult`。
- [x] 定义稳定的 `MaskStrategy` SPI。
- [x] 支持未知类型、空值和短字符串的安全处理语义。
- [x] 核心模型不依赖 Spring MVC、Log4j2 或 report 实现。

## Test requirements

- 为核心模型和策略接口约定编写单元测试。
- 验证 core 模块可独立构建。
