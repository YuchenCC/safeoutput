# R2.5 接入方式场景说明页

> *This was generated by AI during triage.*

- category: enhancement
- state: ready-for-agent
- source: doc/prd/safe-output-r25-prd.md
- wbs: R2.5-02
- blocked_by: 0047-r25-business-workbench-and-mock-domain.md

## What to build

在 Demo 中增加接入方式场景说明页，用业务语言解释每个脱敏能力为什么这样接入。页面和接口应围绕“业务场景 -> 接入方式 -> 示例接口 -> 字段或日志 key -> 规则来源 -> 输出效果”组织，覆盖 YAML、注解、默认规则、ignore、Log4j2 PatternConverter 和主动脱敏服务。

该页不是长篇静态文档，而是可点击、可触发、可对照业务接口结果的接入说明。

## Acceptance criteria

- [ ] 页面列出 Demo 中所有主要业务场景及其接入方式。
- [ ] YAML 配置规则有对应业务字段、示例接口和输出效果。
- [ ] 注解规则有对应歧义字段、示例接口和输出效果。
- [ ] 默认规则库命中有对应字段和输出效果。
- [ ] 字段级 ignore 和接口级 ignore 均有对应场景说明。
- [ ] Log4j2 `%safeOutputMsg` 接入有对应日志场景说明。
- [ ] `SafeOutputMaskService` 主动脱敏接入有对应业务或实验室场景说明。
- [ ] 每条说明能标明规则来源，不把类型标签、字段名和规则名混淆。
- [ ] 页面可从说明项跳转或触发对应 Demo 场景。

## Blocked by

- 0047-r25-business-workbench-and-mock-domain.md

## Test requirements

- 覆盖接入说明接口返回结构的测试。
- 验证说明项覆盖 YAML、注解、默认规则、ignore、Log4j2 和主动脱敏。
- 验证说明中的示例接口或场景标识在 Demo 中存在。
- 运行 `mvn -pl safe-output-demo -am test`。
