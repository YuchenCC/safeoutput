# Safe Output 上下文

Safe Output 是一个面向遗留 Spring Boot 服务的 Java 输出侧数据脱敏组件。本上下文定义 MVP、R2 需求、WBS 和后续设计文档中使用的产品语言。

## Language

**输出侧脱敏**:
当敏感数据通过响应体或日志离开应用时，对其进行掩码处理。
_Avoid_: 输入校验、存储加密

**MVP**:
第一个可交付版本，用于证明在 JDK8 和 Spring Boot 2.x 服务中以低侵入方式实现响应脱敏和 Log4j2 日志脱敏。
_Avoid_: 治理平台、完整安全套件

**MaskType**:
内置标准敏感数据类别，例如手机号、邮箱、中国大陆身份证号或银行卡号。R2 起它不再是业务自定义类型的唯一合法范围。
_Avoid_: 字段名、规则名

**类型标签**:
对外标识脱敏类型的字符串，例如 `MOBILE`、`ID_CARD` 或业务自定义的 `mobileM`。内置 MaskType 或 MaskTypes 常量只是推荐来源。
_Avoid_: Java 枚举约束、字段名

**MaskScene**:
应用脱敏的输出场景，目前包括响应、日志和主动调用。
_Avoid_: 模块、层

**Rule**:
配置或内置的映射，用于决定匹配到的字段、路径、注解或兜底模式应适用哪个类型标签。
_Avoid_: 策略、权限

**Ignore**:
显式豁免项，用于阻止某个字段或接口执行响应脱敏，同时保留所需统计。
_Avoid_: 白名单

**歧义字段**:
类似 `name`、`id`、`code` 或 `no` 这类含义过于模糊、不能安全纳入默认强规则的字段名。
_Avoid_: 敏感字段

**Regex fallback**:
当没有更高优先级规则完成分类时，最后才执行的模式匹配。
_Avoid_: 默认规则

**主动脱敏**:
宿主系统显式调用 Safe Output 提供的服务，对指定值、结构化对象或非结构化文本执行统一脱敏。
_Avoid_: 自动拦截 DAO、MyBatis、缓存、MQ 或文件导出

**MANUAL 场景**:
主动脱敏产生的统计场景，用于区别于自动 Response 脱敏和 Log 脱敏。
_Avoid_: Response 接口风险统计

**接口风险统计**:
响应场景下的聚合指标，用于识别可能暴露敏感数据的接口。
_Avoid_: 审计日志、追踪日志

**治理建议**:
基于聚合统计摘要生成的风险原因、性能提示或规则补齐建议，需要接入方人工确认。
_Avoid_: 自动修改配置、在线决策、权限控制

**报告快照**:
按计划生成的本地 JSON 文件，只包含聚合指标，不包含敏感原始值、完整响应或完整日志。
_Avoid_: 数据转储、样本归档

## Relationships

- 一个 **Rule** 为一个匹配值选择一个 **类型标签**。
- 一个 **MaskScene** 决定使用响应处理规则还是日志处理规则。
- 一个 **Ignore** 的优先级高于注解、路径、键名、默认和兜底规则。
- 一个 **歧义字段** 默认不脱敏，必须由显式 **Rule** 或注解覆盖。
- 一次 **主动脱敏** 计入 **MANUAL 场景**，但不默认进入 **接口风险统计**。
- 一个 **报告快照** 只包含 **接口风险统计**、聚合的类型标签计数和治理建议摘要。

## Example Dialogue

> **Dev:** "字段 `id` 应该被默认规则脱敏吗？"
> **Domain expert:** "不应该。`id` 是一个 **歧义字段**。如果某个具体 API 把它当作敏感字段，必须用显式 **Rule** 覆盖。"

## Flagged Ambiguities

- "ignore" 表示显式脱敏豁免，不表示安全权限或审计绕过。
- "statistics" 表示聚合脱敏指标，不表示存储原始响应或日志样本。
- "default rule" 不同于 **Regex fallback**；fallback 只会在更高优先级匹配失败后执行。
- "custom type" 表示自定义 **类型标签**，不要求加入内置 **MaskType** 枚举。
- "governance advice" 表示人工确认建议，不表示组件会自动修改配置或改变在线接口行为。
